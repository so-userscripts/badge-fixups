{
  "name": "badge-fixups",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "uglify": "uglifyjs dist/* -c -m -o dist/badgefixups.js",
    "build": "rm -rf dist && tsc && npm run uglify && rm dist/main.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/so-userscripts/badge-fixups.git"
  },
  "keywords": [
    "stackoverflow",
    "badges"
  ],
  "author": "Oleg Valter",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/so-userscripts/badge-fixups/issues"
  },
  "homepage": "https://github.com/so-userscripts/badge-fixups#readme",
  "devDependencies": {
    "uglify-js": "^3.13.6"
  }
}
